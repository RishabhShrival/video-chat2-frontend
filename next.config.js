/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Helps catch potential issues in React components
  experimental: {}, // Used for enabling experimental Next.js features
};

module.exports = nextConfig;